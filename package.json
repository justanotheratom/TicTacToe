{
  "private": true,
  "scripts": {
    "start": "dotnet fable watch src --sourceMaps --run vite",
    "build": "dotnet fable src --sourceMaps --run vite build && cp src/staticwebapp.config.json public/",
    "serve": "vite serve"
  },
  "devDependencies": {
    "vite": "^2.4.0"
  },
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "dependencies": {
    "@microsoft/signalr": "^5.0.17",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
